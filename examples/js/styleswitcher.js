/* * Code from A list Apart Article "Alternative Style: Working With Alternate Style Sheets" * http://alistapart.com/article/alternate/ */var altStyleSheet = new function () {  var me = this,      cookie,      title,      isIE;    function init() {    $(document).ready(me.onload);    $(window).bind('unload', me.onunload);        cookie = readCookie("style");    title = cookie ? cookie : me.getPreferred();        isIE = $('body').hasClass('ie9down');  }    me.setActive = function (title) {    var i, a, main;    for(i=0; (a = document.getElementsByTagName("link")[i]); i++) {      if(a.getAttribute("rel").indexOf("style") != -1 && a.getAttribute("title")) {        a.disabled = true;               if (isIE) {          a.setAttribute("rel", 'altstyle');                 }                if(a.getAttribute("title") == title) {          a.disabled = false;                    if (isIE) {                        a.setAttribute("rel", 'stylesheet');                      }        }                      }    }  }    me.getActive = function () {    var i, a;    for(i=0; (a = document.getElementsByTagName("link")[i]); i++) {      if(a.getAttribute("rel").indexOf("style") != -1 && a.getAttribute("title") && !a.disabled) return a.getAttribute("title");    }    return null;  }    me.getPreferred = function () {    var i, a;    for(i=0; (a = document.getElementsByTagName("link")[i]); i++) {      if(a.getAttribute("rel").indexOf("style") != -1         && a.getAttribute("rel").indexOf("alt") == -1         && a.getAttribute("title")         ) return a.getAttribute("title");    }    return null;  }    function createCookie(name,value,days) {    if (days) {      var date = new Date();      date.setTime(date.getTime()+(days*24*60*60*1000));      var expires = "; expires="+date.toGMTString();    }    else expires = "";    document.cookie = name+"="+value+expires+"; path=/";  }    function readCookie(name) {    var nameEQ = name + "=";    var ca = document.cookie.split(';');    for(var i=0;i < ca.length;i++) {      var c = ca[i];      while (c.charAt(0)==' ') c = c.substring(1,c.length);      if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length,c.length);    }    return null;  }    me.onload = function(e) {    var cookie = readCookie("style");    var title = cookie ? cookie : me.getPreferred();    isIE = $('body').hasClass('ie9down');        me.setActive(title);        createSelect();  }    me.onunload = function(e) {    var title = me.getActive();    createCookie("style", title, 365);  }    function createSelect() {    var $widget = $('#styleswitcher-widget');    var sb = [],        activeTitle = me.getActive();        sb.push('<select id="styleswitcher-select">');        $('link[class="styleswitcher-css"][title]').each(      function (i, el) {        var title=el.title;                sb.push('<option value="')        sb.push(title)        sb.push('" ')        sb.push(title==activeTitle?'selected':'')        sb.push('>')        sb.push(title)        sb.push('</option>')      }    );        sb.push('</select>')              $widget.html(sb.join(''));         $('#styleswitcher-select').bind('change', selectChangeEvent);   }    function selectChangeEvent(e) {    var target = e.currentTarget;        me.setActive(target.value);          }    init();  }